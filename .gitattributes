# Normalize line endings so Windows checkouts don't produce whole-file diffs
# for collaborators on macOS/Linux (and vice versa).
* text=auto eol=lf

# Binary assets: never touch these.
*.png binary
*.ico binary
*.jpg binary
*.jpeg binary
*.gif binary
*.webp binary
*.woff binary
*.woff2 binary
